package:
  name: eurec4a_snd
  version: "0.0.4"  # "{{ load_setup_py_data().version }}"

source:
  path: ..

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - pip
    - setuptools
    - setuptools-git-version
    - numpy
    - matplotlib
    - basemap
    - netCDF4
  run:
    - python
    - numpy
    - matplotlib
    - basemap
    - netCDF4